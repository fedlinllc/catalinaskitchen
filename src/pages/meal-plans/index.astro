---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { getAllMealPlans, getImageUrl } from "../../lib/contentful";

const allPlans = await getAllMealPlans();
---

<BaseLayout
  title="Meal Plans"
  description="Weekly meal plans to help you eat healthy, save time, and discover new flavors. Updated regularly by Catalina's Kitchen."
>
  <div class="max-w-6xl mx-auto px-4 py-12">

    <div class="text-center mb-10">
      <h1 class="font-display text-4xl md:text-5xl text-[#0a0a0a] mb-3">Meal Plans</h1>
      <p class="text-[#2a2a2a] max-w-xl mx-auto">
        Structured weekly menus to make healthy eating simple and delicious.
      </p>
    </div>

    {allPlans.length > 0 ? (
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {allPlans.map((plan) => {
          const isCurrent = plan.fields.isCurrent as boolean;
          const imageUrl = getImageUrl(plan.fields.featuredImage as any);
          return (
            <a
              href={`/meal-plans/${plan.fields.slug}`}
              class:list={[
                "group block rounded-2xl overflow-hidden border hover:shadow-md transition-shadow bg-[#f2ece4]",
                isCurrent ? "border-[#111111] ring-2 ring-[#111111]/20" : "border-[#d4c8bc]",
              ]}
            >
              <div class="relative aspect-[4/3] overflow-hidden bg-[#e0d8cc]">
                {imageUrl ? (
                  <img
                    src={imageUrl}
                    alt={plan.fields.title as string}
                    class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                    loading="lazy"
                  />
                ) : (
                  <div class="w-full h-full flex items-center justify-center">
                    <span class="font-display text-5xl text-[#D4B896]">🥗</span>
                  </div>
                )}
                {isCurrent && (
                  <span class="absolute top-3 left-3 bg-[#111111] text-white text-xs font-semibold uppercase tracking-wider px-3 py-1 rounded-full">
                    Current Week
                  </span>
                )}
              </div>
              <div class="p-5">
                <h2 class="font-display text-lg text-[#0a0a0a] group-hover:text-[#111111] transition-colors mb-1">
                  {plan.fields.title}
                </h2>
                <p class="text-xs text-[#6b6b6b]">
                  Week of {new Date(plan.fields.weekOf as string).toLocaleDateString("en-US", {
                    month: "long",
                    day: "numeric",
                    year: "numeric",
                  })}
                </p>
              </div>
            </a>
          );
        })}
      </div>
    ) : (
      <p class="text-center text-[#2a2a2a] py-20">Meal plans coming soon.</p>
    )}

  </div>
</BaseLayout>
